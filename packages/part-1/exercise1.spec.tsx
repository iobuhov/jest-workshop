/**
 * Task:
 * Write a simple spec using `describe` and `it` for
 * our `sum` function.
 * Use "What"->"with/when<optional>"->"behavior" style to write the test.
 */

function sum(a: number, b: number): number {
    return a + b;
}
